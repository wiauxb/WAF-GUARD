fastapi[standard]
neo4j
python-dotenv 
pydantic
pandas==2.2.3 
#psycopg2-binary 
dotenv 
requests 
tqdm
uvicorn #?
# Pinned: create_agent + ToolRuntime (the typed per-invocation context the chatbot binds
# its configuration through) need langchain v1 and a current langgraph. `langchain` is the
# meta package -- langchain-core alone does NOT provide langchain.agents.
langgraph==1.2.11
langchain>=1,<2
streamlit #?
langchain-openai 
langchain-community 
langchain-neo4j 
langchain-chroma 
psycopg 
psycopg-pool 
langgraph-checkpoint-postgres
python-jose[cryptography]
bcrypt>=4.0.1,<6.0.0
python-multipart
pyvis
# PCRE-compatible regex, for matching a URL against <LocationMatch> patterns.
# NOT interchangeable with the stdlib `re`: Apache/PCRE allows inline flags mid-pattern
# (`/(?i)(?:TestSecAllow)?SOAP`) and `re` rejects those -- 56 of one real configuration's
# 545 patterns fail to compile under it. Was already present transitively via tiktoken;
# pinned here so dropping the chatbot's tokenizer cannot silently break URL matching.
regex
# pandashttpx
